<%= form_with(model: audition_request, local: true) do |form| %>
  <% if audition_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(audition_request.errors.count, "error") %> prohibited this audition_request from being saved:</h2>

      <ul>
      <% audition_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :actor_id %>
    <%= form.number_field :actor_id, id: :audition_request_actor_id %>
  </div>

  <div class="field">
    <%= form.label :role_id %>
    <%= form.number_field :role_id, id: :audition_request_role_id %>
  </div>

  <div class="field">
    <%= form.label :accepted? %>
    <%= form.check_box :accepted?, id: :audition_request_accepted? %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
