# Add your own tasks in files placed in lib/tasks ending in .rake,
# for example lib/tasks/capistrano.rake, and they will automatically be available to Rake.

require_relative 'config/application'

# Added so it recognizes the MovieGetter
# require_all 'app'
# require_all 'public/uploads'
# gem 'rest-client'

Rails.application.load_tasks

# desc "Seed the database with movies"
# task :seed_movies do
#   array_of_imdb_ids = %w(tt0335266)
#   array_of_imdb_ids.each do |imdb_id|
#     MovieGetter.new(imdb_id).seed
#   end
# end
